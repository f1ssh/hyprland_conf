env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland
env = XCURSOR_THEME,Bibata-Modern-Classic
env = XCURSOR_SIZE,24



# ---------- MONITORS ----------
# Auto-detect whatever VMware gives us
# monitor=,preferred,1920x1080,1

# ---------- ENV ----------
env = XCURSOR_SIZE,24

# ---------- INPUT ----------
input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = yes
    }
}

# ---------- GENERAL LOOK ----------

#general {
#    gaps_in = 10
#    gaps_out = 20
#    border_size = 2

    # Cream active border, dark inactive border
#    col.active_border = rgb(f0ead2)
#    col.inactive_border = rgb(161614)
#}


#decoration {
#    rounding = 8
#}

#animations {
#    enabled = yes
#    bezier = ease, 0.05, 0.9, 0.1, 1.0
#
#    animation = windows, 1, 7, ease, slide
#    animation = windowsOut, 1, 7, ease, slide
#    animation = border, 1, 10, ease
#    animation = fade, 1, 7, ease
#    animation = workspaces, 1, 6, ease, slidevert
#}


# ---------- AUTOSTART ----------
exec-once = waybar
exec-once = hyprpaper
exec-once = mako


# ---------- KEYBINDS ----------
# Super key is "SUPER"
$mainMod = SUPER

# Launch Rofi
bind = SUPER, SPACE, exec, rofi -show drun

# Launch terminal
bind = $mainMod, Return, exec, kitty

# Close window
bind = $mainMod, Q, killactive

# Fullscreen
bind = $mainMod, F, fullscreen

# Change focus
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move windows
bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# Workspaces 1–9
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9

# Move window to workspace
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9

# Screenshot (full)
bind = $mainMod, S, exec, grim ~/Pictures/screenshot-$(date +'%Y%m%d-%H%M%S').png

# Alt tabbing
bind = ALT, TAB, workspace, previous

# ─────────────────────────────────────────
# Matt Dark Monochrome Theme
# ─────────────────────────────────────────

general {
    gaps_in = 10
    gaps_out = 20
    border_size = 3

    # Cream active border, dark inactive border
    col.active_border   = rgb(f0ead2)
    col.inactive_border = rgb(161614)
}


decoration {
    rounding = 8
    active_opacity   = 1.0
    inactive_opacity = 1.0
    fullscreen_opacity = 1.0
    dim_inactive = false

    blur {
        enabled = false
    }

    shadow {
        enabled = false
    }
}


animations {
    enabled = true
}

# Curve definition
bezier = easeOut, 0.05, 0.9, 0.1, 1.0

# Subtle, not crazy
animation = windows,    1, 6,  easeOut, popin
animation = border,     1, 4,  easeOut
animation = fade,       1, 4,  easeOut
animation = workspaces, 1, 5,  easeOut, slide


# Wallpaper (we'll later swap to your custom lamp PNG)
# exec-once = swww init && swww img ~/Pictures/wallpapers/lamp-on.png --resize fill
